# Pet Translator - Team Tasks & Workload

## 1. Mohamed MELLOUK (25% Contribution)
**Role:** Audio Signal Processing and Pipeline
**Tasks:**
- Data Collection (ESC-50, AudioSet, custom recordings).
- Preprocessing Pipeline (ambient noise reduction using `noisereduce`, Voice Activity Detection).
- Feature Extraction (converting raw audio waves into Mel-spectrograms and MFCCs using `librosa`).

## 2. Anas ISARTI (25% Contribution)
**Role:** Classification Models (MobileNetV2 to TFJS)
**Tasks:**
- Model Fine-Tuning (fine-tuning MobileNetV2 on spectrograms to classify intentions).
- Edge Optimization (converting Keras models to TensorFlow.js format with INT8 quantization).
- Browser Integration (deploying TFJS model for low-latency local inference).

## 3. Abir ISLAM (25% Contribution)
**Role:** LLM Fine-Tuning & Inference (Llama 3.2 + LoRA)
**Tasks:**
- Dataset Generation (creating a synthetic dataset for intentions and personalities).
- Model Fine-Tuning (using LoRA and Unsloth to fine-tune Llama 3.2).
- RAG System & Deployment (building a semantic behavioral knowledge base and converting the model to GGUF for local inference).

## 4. Amine KHALIL (25% Contribution)
**Role:** Frontend Web, Backend API & Full Integration
**Tasks:**
- Frontend UI (building the React web interface with audio capture capabilities).
- Backend Server (developing the FastAPI server).
- Full Integration (connecting the audio capture, TFJS edge classification, and remote LLM generation).

---
**Total Estimated Workload:** 100% (equally distributed)
