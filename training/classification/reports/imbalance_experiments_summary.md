# Class Imbalance Experiments — Focal Loss & SMOTE

I tested two class-imbalance techniques — focal loss and SMOTE — against the cross-entropy baseline on the production MobileNetV2 frozen features. The evaluation protocol is identical to all previous CV runs. This is the fifth and final attempt to improve on the production benchmark.

## Techniques and Parameters

**Focal Loss (condition B)**: gamma=2.0 (standard value from Lin et al. 2017). I do *not* combine focal loss with class_weight, because the modulating factor (1-pt)^2.0 already down-weights easy/frequent examples implicitly — adding class_weight on top would double-count the imbalance correction and risk over-penalising the minority class.

**SMOTE (condition C)**: `sampling_strategy='not majority'`, `k_neighbors=5`. Within each fold's training set, SMOTE brings all non-majority classes up to the majority class count. I keep class_weight='balanced' with SMOTE: after resampling the data is near-balanced, so computed weights are close to 1.0 and do not distort training.

**SMOTE+FOCAL (condition D)**: SMOTE resampling + focal loss (no class_weight). Tests whether the two techniques compound.

## Anti-Leakage Verification

- **Cat group leakage**: 0 cat_id violation(s) across all folds and all conditions (expected 0 — confirmed).
- **SMOTE train-only**: SMOTE is applied inside the CV loop, exclusively on `X[train_idx]`. The validation set always contains only real, original samples. For cat, the group-safe fold split guarantees that no cat_id from the validation fold appears in the training set; therefore synthetic samples generated by SMOTE can only be interpolations of training-fold cats, and carry no individual information from validation cats.
- **Feature extraction**: per-sample min-max normalisation in `spectrograms_to_images` uses each clip's own values only — no cross-sample statistic leaks across folds.


## Results — Dog (StratifiedKFold k=5)

| Condition | Macro-F1 (mean+/-std) | Accuracy (mean+/-std) |
|-----------|----------------------|----------------------|
| BASELINE | 0.8244 +/- 0.1114 | 0.8229 +/- 0.1109 |
| FOCAL | 0.8340 +/- 0.1019 | 0.8312 +/- 0.1053 |
| SMOTE | 0.8345 +/- 0.1145 | 0.8316 +/- 0.1170 |
| SMOTE+FOCAL | 0.8318 +/- 0.0935 | 0.8316 +/- 0.0952 |

### Dog Deltas vs Baseline

- **FOCAL**: +0.0096 (0.09x std -> noise, improves)

- **SMOTE**: +0.0101 (0.09x std -> noise, improves)

- **SMOTE+FOCAL**: +0.0074 (0.07x std -> noise, improves)


## Results — Cat (StratifiedGroupKFold k=4, group=cat_id)

| Condition | Macro-F1 (mean+/-std) | Accuracy (mean+/-std) | Food F1 (mean+/-std) | Food Precision | Food Recall |
|-----------|----------------------|----------------------|---------------------|----------------|-------------|
| BASELINE | 0.5223 +/- 0.1338 | 0.5603 +/- 0.1431 | 0.3646 +/- 0.1896 | 0.3648 +/- 0.2325 | 0.3751 +/- 0.1475 |
| FOCAL | 0.4967 +/- 0.1160 | 0.5786 +/- 0.0901 | 0.3168 +/- 0.2367 | 0.3380 +/- 0.2592 | 0.2991 +/- 0.2192 |
| SMOTE | 0.4718 +/- 0.1239 | 0.5129 +/- 0.1529 | 0.3561 +/- 0.1470 | 0.3128 +/- 0.1218 | 0.4688 +/- 0.2296 |
| SMOTE+FOCAL | 0.4773 +/- 0.1585 | 0.5021 +/- 0.1784 | 0.3777 +/- 0.1697 | 0.3179 +/- 0.1578 | 0.5021 +/- 0.2164 |

### Cat Deltas vs Baseline

- **FOCAL** macro-F1: -0.0256 (0.19x std -> noise, degrades)
  food F1: -0.0477 (0.25x std(RAW) -> noise)

- **SMOTE** macro-F1: -0.0505 (0.38x std -> noise, degrades)
  food F1: -0.0084 (0.04x std(RAW) -> noise)

- **SMOTE+FOCAL** macro-F1: -0.0449 (0.34x std -> noise, degrades)
  food F1: +0.0132 (0.07x std(RAW) -> noise)


## Benchmark Reproduction

- Dog BASELINE macro-F1: 0.8244 +/- 0.1114 (reference: 0.8244 +/- 0.1114)
- Cat BASELINE macro-F1: 0.5223 +/- 0.1338 (reference: 0.5223 +/- 0.1338)

## Final Verdict

No condition beats the baseline by more than one standard deviation on either animal. Every delta is within the noise of fold variance.

**The production model remains unchanged: MobileNetV2 frozen backbone + dense head + cross-entropy + class_weight='balanced'. I do not recommend replacing it.**


---
_Total wall-clock time: 132s_
